# Настройка пользовательского домена для GitHub Pages

## 🚀 Быстрый старт

1. **Настройте DNS** у регистратора домена (см. Шаг 1)
2. **Добавьте домен в GitHub Pages** (см. Шаг 2)
3. **Выполните:**
    ```bash
    ./setup-custom-domain.sh yourdomain.com
    npm run deploy:custom
    ```

## Пошаговая инструкция

### Шаг 1: Настройка DNS записей у вашего провайдера домена

Вам нужно добавить DNS записи у вашего регистратора домена (например, Namecheap, GoDaddy, Cloudflare и т.д.).

#### Вариант A: Использование поддомена (www.example.com)

1. Добавьте **CNAME запись**:
    - **Имя (Name/Host)**: `www` (или оставьте пустым, если нужен корневой домен)
    - **Тип**: `CNAME`
    - **Значение (Value/Target)**: `dolgikhgram.github.io`
    - **TTL**: `3600` (или оставьте по умолчанию)

#### Вариант B: Использование корневого домена (example.com)

Для корневого домена (apex domain) нужно использовать **A записи**:

1. Добавьте **4 A записи** с такими IP адресами:

    ```
    185.199.108.153
    185.199.109.153
    185.199.110.153
    185.199.111.153
    ```

    Для каждой записи:
    - **Имя (Name/Host)**: `@` или оставьте пустым (для корневого домена)
    - **Тип**: `A`
    - **Значение (Value)**: один из IP выше
    - **TTL**: `3600`

**Важно:** IP адреса GitHub Pages могут меняться. Рекомендуется использовать поддомен `www` с CNAME записью.

### Шаг 2: Настройка домена в GitHub Pages

1. **Откройте настройки репозитория:**
    - Перейдите в `Settings` → `Pages`

2. **Добавьте домен:**
    - В разделе `Custom domain` введите ваш домен (например, `www.example.com` или `example.com`)
    - Нажмите **"Save"**

3. **Включите HTTPS (рекомендуется):**
    - Убедитесь, что галочка `Enforce HTTPS` включена
    - GitHub автоматически выпустит SSL сертификат через несколько минут/часов

### Шаг 3: Автоматическая настройка проекта

Используйте готовый скрипт для автоматической настройки:

```bash
# Замените yourdomain.com на ваш домен
./setup-custom-domain.sh yourdomain.com
```

Этот скрипт автоматически:

- Создаст файл `public/CNAME` с вашим доменом
- Обновит `homepage` в `package.json`

**Или настройте вручную:**

1. **Создайте файл `public/CNAME`** с вашим доменом:

    ```
    yourdomain.com
    ```

    или

    ```
    www.yourdomain.com
    ```

2. **Обновите `package.json`:**
    ```json
    "homepage": "https://yourdomain.com"
    ```

### Шаг 4: Деплой

После настройки DNS и добавления домена в GitHub:

```bash
# Для ручного деплоя с пользовательским доменом
npm run deploy:custom

# Или для автоматического деплоя (GitHub Actions)
# Добавьте секрет VITE_BASE_PATH в GitHub Secrets со значением "/"
# Workflow автоматически использует его при сборке
git add .
git commit -m "Configure custom domain"
git push origin main
```

**Важно:** Файл `public/CNAME` автоматически копируется в `dist` при сборке проекта благодаря Vite.

## Проверка настройки

### Проверка DNS записей

Проверьте, что DNS записи настроены правильно:

```bash
# Для CNAME (поддомен www)
dig www.yourdomain.com CNAME

# Для A записей (корневой домен)
dig yourdomain.com A
```

Или используйте онлайн инструменты:

- https://mxtoolbox.com/
- https://www.whatsmydns.net/

### Проверка SSL сертификата

После настройки домена GitHub автоматически выпустит SSL сертификат через некоторое время (обычно до 24 часов). Проверьте в настройках Pages, что статус SSL сертификата активен.

## Важные моменты

1. **DNS propagation:** После настройки DNS записей изменения могут распространяться от нескольких минут до 48 часов. Обычно это занимает 1-2 часа.

2. **HTTPS:** GitHub автоматически настраивает HTTPS для пользовательских доменов. Это может занять некоторое время после добавления домена.

3. **WWW vs non-WWW:** Рекомендуется использовать поддомен `www` (например, `www.example.com`), так как это проще настроить через CNAME запись.

4. **Проверка домена:** GitHub проверит, что DNS записи настроены правильно, прежде чем активировать домен.

## Устранение проблем

### Домен не активируется

1. Проверьте DNS записи через `dig` или онлайн инструменты
2. Убедитесь, что DNS записи точно соответствуют инструкции выше
3. Подождите до 24 часов для распространения DNS изменений

### SSL сертификат не выдается

1. Убедитесь, что DNS записи настроены правильно
2. Убедитесь, что домен активен в настройках GitHub Pages
3. Подождите до 24 часов - GitHub выпускает сертификаты автоматически

### Сайт не загружается

1. Проверьте, что `base` в `vite.config.ts` установлен в `'/'`
2. Проверьте, что домен правильно указан в `package.json`
3. Убедитесь, что файл `CNAME` (если используется) содержит правильный домен

## Примеры для популярных регистраторов

### Cloudflare

1. Зайдите в DNS настройки домена
2. Добавьте CNAME запись:
    - Name: `www`
    - Target: `dolgikhgram.github.io`
    - Proxy: выключен (серый облачко)

### Namecheap

1. Зайдите в Advanced DNS
2. Добавьте CNAME запись:
    - Host: `www`
    - Value: `dolgikhgram.github.io`
    - TTL: Automatic

### GoDaddy

1. Зайдите в DNS Management
2. Добавьте CNAME запись:
    - Type: CNAME
    - Name: `www`
    - Value: `dolgikhgram.github.io`
    - TTL: 1 hour
