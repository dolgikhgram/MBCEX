# Исправление ошибки NotServedByPagesError для mbcex.ru

## 🔴 Проблема

Ошибка: `NotServedByPagesError` - GitHub Pages не может найти правильные DNS записи для домена mbcex.ru

**Текущая ситуация:**

- ✅ `www.mbcex.ru` - настроен правильно (CNAME → dolgikhgram.github.io)
- ❌ `mbcex.ru` (корневой домен) - **НЕТ A записей**

## ✅ Решение

### Вариант 1: Добавить A записи для корневого домена (рекомендуется)

Если вы хотите использовать `mbcex.ru` (без www), нужно добавить **4 A записи** у вашего регистратора домена.

#### Шаг 1: Настройка DNS записей

У вашего регистратора домена (REG.RU, Timeweb, Beget и т.д.) добавьте **4 A записи**:

**Запись 1:**

- Тип: `A`
- Имя (Host/Name): `@` или оставьте пустым (для корневого домена)
- Значение (Value/Points to): `185.199.108.153`
- TTL: `3600` или автоматически

**Запись 2:**

- Тип: `A`
- Имя: `@` или пустое
- Значение: `185.199.109.153`
- TTL: `3600`

**Запись 3:**

- Тип: `A`
- Имя: `@` или пустое
- Значение: `185.199.110.153`
- TTL: `3600`

**Запись 4:**

- Тип: `A`
- Имя: `@` или пустое
- Значение: `185.199.111.153`
- TTL: `3600`

#### Шаг 2: Убедитесь, что нет конфликтующих записей

- ❌ Удалите все CNAME записи для `mbcex.ru` (корневой домен не может иметь CNAME)
- ✅ Убедитесь, что A записи добавлены именно для корневого домена (`@` или пустое имя)

#### Шаг 3: Проверка DNS записей

Подождите 5-30 минут и проверьте:

```bash
dig mbcex.ru A +short
```

Должны вернуться 4 IP адреса:

```
185.199.108.153
185.199.109.153
185.199.110.153
185.199.111.153
```

#### Шаг 4: Настройка в GitHub Pages

1. Откройте репозиторий на GitHub
2. Перейдите в `Settings` → `Pages`
3. В разделе `Custom domain` убедитесь, что указан `mbcex.ru`
4. Нажмите **"Save"** (если домен уже добавлен)
5. Нажмите **"Check again"** для проверки DNS

#### Шаг 5: Деплой

После того как DNS записи настроены и GitHub проверил домен:

```bash
npm run deploy:custom
```

Или просто дождитесь автоматического деплоя через GitHub Actions при следующем push.

---

### Вариант 2: Использовать только www.mbcex.ru

Если вы не хотите настраивать A записи для корневого домена, можно использовать только `www.mbcex.ru`:

#### Шаг 1: Обновите CNAME файл

```bash
echo "www.mbcex.ru" > public/CNAME
```

#### Шаг 2: В GitHub Pages

1. `Settings` → `Pages` → `Custom domain`
2. Укажите `www.mbcex.ru` (вместо `mbcex.ru`)
3. Нажмите **"Save"**

#### Шаг 3: Настройте редирект (опционально)

У вашего регистратора домена настройте редирект с `mbcex.ru` на `www.mbcex.ru`. Это позволит пользователям автоматически перенаправляться на www версию.

#### Шаг 4: Деплой

```bash
npm run deploy:custom
```

---

## 📋 Инструкции для популярных регистраторов

### REG.RU (российский регистратор)

1. Зайдите в "Мои домены" → выберите `mbcex.ru`
2. Перейдите в "DNS серверы и зона"
3. В разделе "Записи" добавьте 4 A записи:
    - Нажмите "Добавить запись"
    - Тип: `A`
    - Поддомен: `@` (или оставьте пустым)
    - IP-адрес: `185.199.108.153` (для первой записи)
    - TTL: `3600`
    - Нажмите "Добавить"
    - Повторите для остальных 3 IP адресов

### Timeweb

1. Зайдите в панель управления → выберите домен `mbcex.ru`
2. Перейдите в "DNS-записи" или "Зона DNS"
3. Добавьте 4 A записи:
    - Тип: `A`
    - Имя: `@` или пустое
    - Значение: `185.199.108.153` (первая запись)
    - TTL: `3600`
    - Повторите для остальных 3 IP

### Beget

1. Зайдите в панель управления → "Домены" → `mbcex.ru`
2. Перейдите в "DNS записи"
3. Добавьте 4 A записи:
    - Тип: `A`
    - Поддомен: `@`
    - IP: `185.199.108.153` (для первой)
    - Повторите для остальных 3 IP

### Cloudflare

**Важно:** Если домен использует Cloudflare, нужно отключить Proxy (серый облачко) для A записей, иначе GitHub не сможет их проверить.

1. Зайдите в Cloudflare Dashboard → выберите домен `mbcex.ru`
2. Перейдите в "DNS" → "Records"
3. Добавьте 4 A записи:
    - Type: `A`
    - Name: `@` или корневой домен
    - IPv4 address: `185.199.108.153` (для первой)
    - Proxy status: **DNS only** (серый облачко) - **ВАЖНО!**
    - TTL: Auto
    - Повторите для остальных 3 IP

---

## 🔍 Проверка правильности настройки

### Проверка A записей для mbcex.ru:

```bash
dig mbcex.ru A +short
```

Должно вернуться:

```
185.199.108.153
185.199.109.153
185.199.110.153
185.199.111.153
```

### Проверка CNAME для www.mbcex.ru:

```bash
dig www.mbcex.ru CNAME +short
```

Должно вернуться:

```
dolgikhgram.github.io.
```

### Онлайн проверка:

- https://www.whatsmydns.net/#A/mbcex.ru
- https://mxtoolbox.com/SuperTool.aspx?action=a%3ambcex.ru
- https://dnschecker.org/#A/mbcex.ru

---

## ⚠️ Важные моменты

1. **DNS propagation:** После добавления A записей изменения могут распространяться от 5 минут до 48 часов. Обычно это занимает 15-30 минут.

2. **HTTPS:** GitHub автоматически настраивает HTTPS для пользовательских доменов. Это может занять несколько часов после правильной настройки DNS.

3. **Проверка в GitHub:** После настройки DNS подождите 10-30 минут, затем в GitHub Pages нажмите "Check again". GitHub проверит DNS записи и активирует домен.

4. **Конфликты записей:** Убедитесь, что для корневого домена нет CNAME записей. Корневой домен может иметь только A записи.

---

## 🚨 Если проблема не решается

1. **Проверьте актуальные IP адреса GitHub Pages:**

    ```bash
    dig dolgikhgram.github.io A +short
    ```

    Используйте полученные IP адреса для A записей.

2. **Убедитесь, что файл CNAME содержит правильный домен:**

    ```bash
    cat public/CNAME
    ```

    Должно быть: `mbcex.ru` или `www.mbcex.ru`

3. **Проверьте настройки GitHub Pages:**
    - Repository → Settings → Pages
    - Убедитесь, что Source правильно настроен (GitHub Actions)
    - Убедитесь, что Custom domain указан правильно

4. **Подождите дольше:** Иногда DNS propagation может занять до 24-48 часов.

---

## 📞 Контакты поддержки

Если проблема не решается:

- Проверьте документацию GitHub: https://docs.github.com/en/pages/configuring-a-custom-domain-for-your-github-pages-site
- Обратитесь в поддержку вашего регистратора домена для проверки настроек DNS
- Проверьте GitHub Community: https://github.community/

